<ul>
  <% if Admin::VolvosController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_volvos_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_volvo_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::VolvosController.sortable? and Volvo.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Volvos"),
                 admin_volvos_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Volvos"),
                 admin_volvos_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>