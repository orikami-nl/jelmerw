<ul>
  <% if Admin::AudisController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_audis_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_audi_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::AudisController.sortable? and Audi.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Audis"),
                 admin_audis_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Audis"),
                 admin_audis_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>